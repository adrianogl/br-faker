node_modules/
dist/

# Built by `npm run workflow` / `npm run extension` and published as release
# assets, not committed: both are zips, so every rebuild would be a new blob in
# history even when nothing changed.
br-faker.alfredworkflow
br-faker-extension.zip
*.log
.DS_Store
